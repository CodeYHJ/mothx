# Dynamic Workflows Proposal

This proposal has been superseded by `docs/proposal/dynamic-workflows-javascript-proposal.md`.

The workflow implementation now uses Goja JavaScript rather than the former interpreter design. See the JavaScript proposal and the current workflow reference for the supported DSL, execution model, cancellation, persistence, concurrency, and result fan-in semantics.

{
  "name": "mothx-serve-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "test": "node --test src/lib/*.test.js",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "e2e": "npm test && npm run build && node e2e/channel-settings.smoke.mjs"
  },
  "dependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "vite": "^6.0.0",
    "svelte": "^5.0.0"
  },
  "devDependencies": {}
}

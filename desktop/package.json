{
  "name": "mothx-desktop",
  "version": "1.1.76",
  "private": true,
  "description": "MothX desktop client",
  "type": "module",
  "main": "dist/main.cjs",
  "mothxRuntime": { "version": "1.1.76" },
  "scripts": {
    "vendor": "tsx scripts/vendor-mothx.ts",
    "build": "tsx scripts/build.ts",
    "start": "npm run ensure:electron && npm run vendor && npm run build && electron .",
    "ensure:electron": "node scripts/ensure-electron.cjs",
    "dist": "npm run ensure:electron && npm run build && electron-builder --config electron-builder.yml",
    "dist:dir": "npm run ensure:electron && npm run build && electron-builder --dir --config electron-builder.yml",
    "dist:dev:mac": "npm run ensure:electron && npm run build && electron-builder --mac --publish never --config electron-builder.yml",
    "dist:dev:win": "npm run ensure:electron && npm run build && electron-builder --win --publish never --config electron-builder.yml",
    "dist:dev:linux": "npm run ensure:electron && npm run build && electron-builder --linux --publish never --config electron-builder.yml",
    "dist:mac": "npm run ensure:electron && npm run build && electron-builder --mac --config electron-builder.yml",
    "dist:win": "npm run ensure:electron && npm run build && electron-builder --win --config electron-builder.yml",
    "dist:linux": "npm run ensure:electron && npm run build && electron-builder --linux --config electron-builder.yml",
    "version:set": "node scripts/set-version.cjs",
    "version:check": "tsx scripts/check-release-version.ts"
  },
  "devDependencies": {
    "electron": "^39.2.7",
    "electron-builder": "^26.0.12",
    "electron-updater": "^6.6.2",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
